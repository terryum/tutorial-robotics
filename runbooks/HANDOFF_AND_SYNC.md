# Compatibility Note

There is no one-way MacBook → WS2 handoff in v4. Use [`GIT_SYNC.md`](GIT_SYNC.md) to switch in either direction.

Candidate model promotion from a development host to a robot runtime still requires the complete deployment bundle defined in `templates/DEPLOYMENT_BUNDLE_MANIFEST_TEMPLATE.md`.
